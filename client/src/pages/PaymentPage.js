import React from "react";
import { Layout } from "../component/Layout";

const PaymentPage = () => {
  return (
    <Layout>
      <h2 className="text-center">Payment Page</h2>
      <button onClick>Payment Approved</button>
    </Layout>
  );
};

export default PaymentPage;
