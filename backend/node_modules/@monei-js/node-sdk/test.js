const {Monei} = require('./dist/monei');

const monei = new Monei('pk_live_cc08de38136fddbd368cd0303ac2cc73');

monei.payments
  .create({
    orderId: '123456789',
    amount: 100,
    currency: 'EUR',
    metadata: {
      customId: '123456789'
    }
  })
  .then(console.log)
  .catch(console.error);
